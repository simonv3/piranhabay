{{! This is the post loop - each post will be output using this markup }}
{{#foreach posts}}
<article class="{{post_class}}">
    <time class="post-date" datetime="{{date format='YYYY-MM-DD'}}">{{date format="DD MMM"}}</time>
    <section class="post-details">
        <header class="post-header">
            <h2 class="post-title"><a href="{{url}}" title="{{{title}}}">{{{title}}}</a></h2>
        </header>
        <span class="author">by {{author}}</span>
        <div class="tags">
            {{tags separator=""}}
        </div>
    </section>
    <!-- <section class="post-excerpt">
        <p>{{excerpt words="26"}} <a class="read-more" href="{{url}}">&raquo;</a></p>
    </section>
        {{#if author.image}}<img class="author-thumb" src="{{author.image}}" alt="Author image" nopin="nopin" />{{/if}}

        <time class="post-date" datetime="{{date format='YYYY-MM-DD'}}">{{date format="DD MMM"}}</time>
    </footer>-->

</article>
{{/foreach}}

{{! Previous/next page links - displayed on every page }}
{{pagination}}
